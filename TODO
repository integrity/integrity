- Integrity::SCM should recognize uri that start with git:// too (not just /\.git$/)
- Rename commit_metadata to commit_meta_data
- use trick to pass private methods instead of #send
- Build#output should be allowed to be blank
- Fix test for that we captuire both STDOUT and STDERR
